//the sum of the element  in the arry
#include<iostream>
using namespace std;


int main(){
      /*
      // initialise array
      int arr[] = {2, 7, 1,-4, 11};
      int size = 5;

      // initialise sum to zero
      int sum = 0;

      // for loop runs from 0 to size - 1
      for(int i = 0; i < size; i++)
      {
        sum = sum + arr[i];
      }

      cout << "The sum of the elements in the array: " << sum;
    }      */

    // print the elemen in the arry

     int marks[] = {2,4,5,6};
     int size = 4;

       for(int i =0;i<size;i++)
       {
         cout<< marks[i]<<endl;
       cout << "print the element in arry"<<endl;
       }
       return 0;
      }
